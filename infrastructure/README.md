# Infrastructure

Environment-specific platform definitions will live here. TASK-001 provides local services under `docker/`; deployment assets remain intentionally empty until requirements are established.

